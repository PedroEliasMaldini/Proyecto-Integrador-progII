<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel='stylesheet' href='/stylesheets/style.css' />

</head>

<body>
    
    <%- include('./partials/header') %>

    <body>
        <body>
            <div class="container my-5 d-flex justify-content-center">
              <div class="card" style="width: 30rem;">
                <div class="card-header text-center bg-primary text-white">
                  <h1 class="card-title"><%= results.name %></h1>
                </div>
                <div class="card-body">
                  <p class="card-text"><strong>Descripción:</strong> <%= results.description %></p>
                  <ul class="list-group list-group-flush">
                    <li class="list-group-item"><strong>Marca:</strong> <%= results.brand %></li>
                    <li class="list-group-item"><strong>Modelo:</strong> <%= results.model %></li>
                    <li class="list-group-item"><strong>Color:</strong> <%= results.color %></li>
                    <li class="list-group-item"><strong>Gama:</strong> <%= results.gama %></li>
                    <li class="list-group-item">
                      <strong>Precio:</strong> 
                      <span class="text-success font-weight-bold">
                        <%= new Intl.NumberFormat('en-US', { style: 'currency', currency: 'USD', minimumFractionDigits: 0, maximumFractionDigits: 0 }).format(results.price) %>
                      </span>
                    </li>
                  </ul>
                </div>
                <div class="card-footer text-center">
                  <a href="/home/marca/<%= results.brand %>" class="btn btn-primary mr-2">Ver más productos de esta marca</a>
                  <a href="/home" class="btn btn-secondary">Volver al inicio</a>
                  <button type="button" class="btn btn-outline-danger mt-2">Comprar</button>
                </div>
              </div>
            </div>
        <%- include('./partials/footer') %>
            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
                integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
                crossorigin="anonymous"></script>
</body>

</html>