<!DOCTYPE html>
<html lang="es">
<!-- Include Head -->

<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<link width="" rel="shortcut icon" href="./images/logo-mercado-liebre.jpg" />

	<title> Mercado Liebre Argentina</title>
	<!-- Font Awesome Stylesheet -->
	<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.11.2/css/all.min.css">
	<!-- Bootstrap Grid Stylesheet -->
	<link rel="stylesheet" href="/css/bootstrap-grid.min.css">
	<!-- Own Stylesheet -->
	<link rel="stylesheet" href="/css/app.css">
</head>

<body>
	<% if (user) { %>
           <%- include('./partials/headerLogueado'); %>
        <% } else { %>
           <%- include('./partials/header'); %>
        <% } %>

	<!-- Novedades -->
	<main class="container">
        <section class="profile">
        <h2>Información de <%= usuario.username %></h2>
        <ul>
                <li><strong>Nombre:</strong> <%= usuario.username %></li>
                <li><strong>Email:</strong> <%= usuario.email %></li>
        </ul>
        </section>
		<section>
			<div class="container products-wrapper">
				<div class="row">
					<div class="col-12">
						<h2 class="products-title">Productos</h2>
					</div>
					<% if (productos.length > 0) { %>
						<% for (let i = 0; i < productos.length; i++) { %>
							<div class="col-12 col-sm-6 col-lg-3">
								<section class="product-box">
									<a href="/detalle/<%= productos[i].id %>">
										<figure class="product-box_image">
											<img src="/images/products/<%= productos[i].imagen %>" alt="<%= productos[i].imagen %>">
										</figure>
										<article class="product-box_data">
											<h2><%= productos[i].nombreProducto %></h2>
											<p>
												Valor: 
												<%= new Intl.NumberFormat('en-US', { style: 'currency', currency: 'USD', minimumFractionDigits: 0 }).format(productos[i].precio) %>
											</p>
										</article>
									</a>
								</section>
							</div>
						<% } %>
					<% } else { %>
						<div class="col-12">
							<p>Este usuario no tiene productos disponibles.</p>
						</div>
					<% } %>
				</div>
			</div>
			

  </main>
<!-- footer -->
<%- include('./partials/footer') %>
<!-- /footer -->
</body>

</html>